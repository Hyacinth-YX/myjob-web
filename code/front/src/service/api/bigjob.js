import { instance } from "../utils/http"; // 导入http中创建的axios实例

// getExperimentPageContent(experimentName, page) {
//     return instance.get(
//       `/api/experiment/getPage?experimentName=${experimentName}&page=${page}`
//     );
//   },
const bigjob = {

}